---
title: "Manage authentication"
description: "Use provider API keys or supported subscription logins while keeping credentials out of source control."
sidebar:
  order: 3
  label: "Manage authentication"
---

import { Aside, Steps, TabItem, Tabs } from '@astrojs/starlight/components';

Use provider API keys or supported subscription logins while keeping credentials out of source control.

## Prerequisites

- A current xcsh release and a shell in the intended working directory.
- Authorization for any account, tenant, repository, or file the procedure touches.
- A backup or dry run before an operation that changes or deletes state.

## Procedure

<Steps>

1. Prefer provider-specific environment variables for API-key authentication.
2. Use `/login`, `/logout`, or `/reauth` for supported OAuth/subscription providers.
3. Verify the selected account before using a paid or enterprise route.
4. Remove stale credentials through the same login flow that created them.

</Steps>

## Expected result

The command or interface reports completion in the selected scope. For automation, prefer its structured result instead of parsing decorated terminal output.

## Troubleshooting

Confirm the installed version and resolved configuration first. Retry with optional plugins, MCP servers, or extensions disabled when they could affect the path. Never print tokens while collecting diagnostics.

## Next steps

Continue to [Environment variable reference](../environment-variables/).
