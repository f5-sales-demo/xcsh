---
title: "Resolve secrets safely"
description: "Reference sensitive values without placing plaintext credentials in prompts, manifests, logs, or commits."
sidebar:
  order: 6
  label: "Resolve secrets safely"
---

import { Aside, Steps, TabItem, Tabs } from '@astrojs/starlight/components';

Reference sensitive values without placing plaintext credentials in prompts, manifests, logs, or commits.

## Prerequisites

- A current xcsh release and a shell in the intended working directory.
- Authorization for any account, tenant, repository, or file the procedure touches.
- A backup or dry run before an operation that changes or deletes state.

## Procedure

<Steps>

1. Store project secret mappings in `.xcsh/secrets.yml` only when the repository policy permits and the backing values remain external.
2. Pass live credentials through the environment or protected runtime files.
3. Review generated reports and exports before sharing them.
4. Rotate or remove credentials through their owning provider.

</Steps>

## Expected result

The command or interface reports completion in the selected scope. For automation, prefer its structured result instead of parsing decorated terminal output.

## Troubleshooting

Confirm the installed version and resolved configuration first. Retry with optional plugins, MCP servers, or extensions disabled when they could affect the path. Never print tokens while collecting diagnostics.

## Next steps

Continue to [Sandbox and filesystem boundaries](../sandbox-filesystem/).
