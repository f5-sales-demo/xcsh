---
title: "Contribute"
description: "Use the repository's issue, worktree, validation, review, pull request, and release governance."
sidebar:
  order: 6
  label: "Contribute"
---

import { Aside, Steps, TabItem, Tabs } from '@astrojs/starlight/components';

Use the repository's issue, worktree, validation, review, pull request, and release governance.

## Prerequisites

- A current xcsh release and a shell in the intended working directory.
- Authorization for any account, tenant, repository, or file the procedure touches.
- A backup or dry run before an operation that changes or deletes state.

## Procedure

<Steps>

1. Read `AGENTS.md`, `CONTRIBUTING.md`, and `DEVELOPING.md`.
2. Open a detailed issue and branch from the current `origin/main` in an isolated worktree.
3. Run focused and full validation, inspect the complete diff, and protect credentials and personal data.
4. Deliver through a linked pull request and clean only the verified task resources after merge.

</Steps>

## Expected result

The command or interface reports completion in the selected scope. For automation, prefer its structured result instead of parsing decorated terminal output.

## Troubleshooting

Confirm the installed version and resolved configuration first. Retry with optional plugins, MCP servers, or extensions disabled when they could affect the path. Never print tokens while collecting diagnostics.

## Next steps

Continue to [Documentation coverage matrix](../coverage-matrix/).
