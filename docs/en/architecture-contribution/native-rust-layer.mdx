---
title: "Native Rust layer"
description: "Understand the N-API acceleration layer for PTYs, processes, search, media, and system utilities."
sidebar:
  order: 2
  label: "Native Rust layer"
---

import { Aside, Steps, TabItem, Tabs } from '@astrojs/starlight/components';

Understand the N-API acceleration layer for PTYs, processes, search, media, and system utilities.

## Prerequisites

- A current xcsh release and a shell in the intended working directory.
- Authorization for any account, tenant, repository, or file the procedure touches.
- A backup or dry run before an operation that changes or deletes state.

## Procedure

<Steps>

1. Treat TypeScript bindings as the public boundary.
2. Build with `bun run build:native` after Rust changes.
3. Run `bun run check:rs` and `bun run test:rs`.
4. Verify packaged native artifacts for every supported release target.

</Steps>

## Expected result

The command or interface reports completion in the selected scope. For automation, prefer its structured result instead of parsing decorated terminal output.

## Troubleshooting

Confirm the installed version and resolved configuration first. Retry with optional plugins, MCP servers, or extensions disabled when they could affect the path. Never print tokens while collecting diagnostics.

## Next steps

Continue to [Extensibility boundaries](../extensibility-boundaries/).
