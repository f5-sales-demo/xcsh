---
title: "Create, apply, and update resources"
description: "Choose strict creation, convergent apply, or explicit update for a reviewed manifest."
sidebar:
  order: 4
  label: "Create, apply, and update resources"
---

import { Aside, Steps, TabItem, Tabs } from '@astrojs/starlight/components';

Choose strict creation, convergent apply, or explicit update for a reviewed manifest.

## Prerequisites

- A current xcsh release and a shell in the intended working directory.
- Authorization for any account, tenant, repository, or file the procedure touches.
- A backup or dry run before an operation that changes or deletes state.

## Procedure

<Steps>

1. Use `create` when an existing resource must be an error.
2. Use `apply` to create missing resources, update differences, and preserve matches.
3. Use `update` when the resource must already exist.
4. Add `--dry-run=client` before a live mutation and retain `--result-file` output in automation.

</Steps>

## Expected result

The command or interface reports completion in the selected scope. For automation, prefer its structured result instead of parsing decorated terminal output.

## Troubleshooting

Confirm the installed version and resolved configuration first. Retry with optional plugins, MCP servers, or extensions disabled when they could affect the path. Never print tokens while collecting diagnostics.

## Next steps

Continue to [Get, export, and delete resources](../get-export-delete/).
