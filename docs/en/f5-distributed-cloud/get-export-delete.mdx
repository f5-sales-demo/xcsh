---
title: "Get, export, and delete resources"
description: "Inspect live resources, produce portable manifests, and perform deliberate deletion."
sidebar:
  order: 5
  label: "Get, export, and delete resources"
---

import { Aside, Steps, TabItem, Tabs } from '@astrojs/starlight/components';

Inspect live resources, produce portable manifests, and perform deliberate deletion.

## Prerequisites

- A current xcsh release and a shell in the intended working directory.
- Authorization for any account, tenant, repository, or file the procedure touches.
- A backup or dry run before an operation that changes or deletes state.

## Procedure

<Steps>

1. Run `xcsh get <kind> [name] -n example-namespace`.
2. Export one resource with `xcsh export <kind> <name> -o yaml -f export.yaml`.
3. Review exported metadata before version control.
4. Preview deletion with `xcsh delete -f manifest.yaml --dry-run=client`, then remove the dry-run flag only after confirming scope.

</Steps>

## Expected result

The command or interface reports completion in the selected scope. For automation, prefer its structured result instead of parsing decorated terminal output.

## Troubleshooting

Confirm the installed version and resolved configuration first. Retry with optional plugins, MCP servers, or extensions disabled when they could affect the path. Never print tokens while collecting diagnostics.

## Next steps

Continue to [Understand the API catalog](../api-catalog/).
