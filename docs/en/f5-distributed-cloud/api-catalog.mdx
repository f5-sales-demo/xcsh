---
title: "Understand the API catalog"
description: "See how xcsh resolves resource kinds and validation metadata from its packaged F5 Distributed Cloud catalog."
sidebar:
  order: 6
  label: "Understand the API catalog"
---

The catalog maps a supported `kind` to its API path and schema handling. It is the authority for whether a resource can use deterministic commands.

## How do I choose a supported kind?

Use catalog discovery in the interactive shell, or inspect the generated resource catalog in the matching xcsh source release. Copy the exact `kind` spelling into the manifest.

## What if a kind is absent?

Do not substitute a similar name. Use console automation only when that interface is explicitly supported, or wait for catalog support. An absent kind cannot be made safe by adding arbitrary fields to a manifest.
