---
title: "Configure MCP transports"
description: "Control MCP server transport, authentication, lifecycle, roots, tools, resources, and prompts."
sidebar:
  order: 4
  label: "Configure MCP transports"
---

import { Aside, Steps, TabItem, Tabs } from '@astrojs/starlight/components';

Control MCP server transport, authentication, lifecycle, roots, tools, resources, and prompts.

## Prerequisites

- A current xcsh release and a shell in the intended working directory.
- Authorization for any account, tenant, repository, or file the procedure touches.
- A backup or dry run before an operation that changes or deletes state.

## Procedure

<Steps>

1. Store project servers in `.xcsh/mcp.json` and user servers in the user configuration tree.
2. Use stdio for child processes, or HTTP/SSE for reachable services.
3. Reference tokens through the environment when possible.
4. Restart or reload after changes and test a harmless server capability.

</Steps>

## Expected result

The command or interface reports completion in the selected scope. For automation, prefer its structured result instead of parsing decorated terminal output.

## Troubleshooting

Confirm the installed version and resolved configuration first. Retry with optional plugins, MCP servers, or extensions disabled when they could affect the path. Never print tokens while collecting diagnostics.

## Next steps

Continue to [Build extensions and custom tools](../extensions-custom-tools/).
