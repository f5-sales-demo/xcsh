---
title: "Fix context and tool problems"
description: "Separate F5 Distributed Cloud scope errors from MCP, browser, Python, and shell-tool failures."
sidebar:
  order: 3
  label: "Fix context and tool problems"
---

import { Aside, Steps, TabItem, Tabs } from '@astrojs/starlight/components';

Separate F5 Distributed Cloud scope errors from MCP, browser, Python, and shell-tool failures.

## Prerequisites

- A current xcsh release and a shell in the intended working directory.
- Authorization for any account, tenant, repository, or file the procedure touches.
- A backup or dry run before an operation that changes or deletes state.

## Procedure

<Steps>

1. Check `/context status` and the explicit namespace first.
2. Use `validate` to rule out local manifest problems.
3. Check optional components with `xcsh setup <component> --check`.
4. Disable MCP, extensions, or the PTY one at a time to isolate the failing layer.

</Steps>

## Expected result

The command or interface reports completion in the selected scope. For automation, prefer its structured result instead of parsing decorated terminal output.

## Troubleshooting

Confirm the installed version and resolved configuration first. Retry with optional plugins, MCP servers, or extensions disabled when they could affect the path. Never print tokens while collecting diagnostics.

## Next steps

Continue to [CLI command and flag reference](../cli-reference/).
