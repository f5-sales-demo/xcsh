---
title: "Authenticate a model provider"
description: "Configure an API key or supported subscription login without placing credentials in project files."
sidebar:
  order: 4
  label: "Authenticate a model provider"
---

import { Aside, Steps, TabItem, Tabs } from '@astrojs/starlight/components';

Configure an API key or supported subscription login without placing credentials in project files.

## Prerequisites

- A current xcsh release and a shell in the intended working directory.
- Authorization for any account, tenant, repository, or file the procedure touches.
- A backup or dry run before an operation that changes or deletes state.

## Procedure

<Steps>

1. Choose a provider shown by `xcsh --list-models all`.
2. Export that provider's documented environment variable or use `/login` for a supported subscription provider.
3. Run `/model` and select a model.
4. Send a short prompt to verify streaming.

</Steps>

## Expected result

The command or interface reports completion in the selected scope. For automation, prefer its structured result instead of parsing decorated terminal output.

## Troubleshooting

Confirm the installed version and resolved configuration first. Retry with optional plugins, MCP servers, or extensions disabled when they could affect the path. Never print tokens while collecting diagnostics.

## Next steps

Continue to [Connect to F5 Distributed Cloud](../f5-xc-connection/).
