---
title: "Understand settings precedence"
description: "Predict which user, project, environment, and command-line value xcsh resolves."
sidebar:
  order: 1
  label: "Understand settings precedence"
---

import { Aside, Steps, TabItem, Tabs } from '@astrojs/starlight/components';

Predict which user, project, environment, and command-line value xcsh resolves.

## Prerequisites

- A current xcsh release and a shell in the intended working directory.
- Authorization for any account, tenant, repository, or file the procedure touches.
- A backup or dry run before an operation that changes or deletes state.

## Procedure

<Steps>

1. Inspect effective values with `xcsh config list` or `xcsh config get <key>`.
2. Use project `.xcsh/settings.json` for shareable project behavior and user settings for personal defaults.
3. Apply environment or CLI overrides for a single execution.
4. Record security-relevant exceptions next to the scope that requires them.

</Steps>

## Expected result

The command or interface reports completion in the selected scope. For automation, prefer its structured result instead of parsing decorated terminal output.

## Troubleshooting

Confirm the installed version and resolved configuration first. Retry with optional plugins, MCP servers, or extensions disabled when they could affect the path. Never print tokens while collecting diagnostics.

## Next steps

Continue to [Configure providers and model routing](../providers-routing/).
