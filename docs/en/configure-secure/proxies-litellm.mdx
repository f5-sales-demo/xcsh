---
title: "Use proxies and LiteLLM"
description: "Route compatible models through a controlled gateway and separate endpoint configuration from credentials."
sidebar:
  order: 5
  label: "Use proxies and LiteLLM"
---

import { Aside, Steps, TabItem, Tabs } from '@astrojs/starlight/components';

Route compatible models through a controlled gateway and separate endpoint configuration from credentials.

## Prerequisites

- A current xcsh release and a shell in the intended working directory.
- Authorization for any account, tenant, repository, or file the procedure touches.
- A backup or dry run before an operation that changes or deletes state.

## Procedure

<Steps>

1. Install optional local support with `xcsh setup litellm` when required.
2. Configure the gateway URL and key through approved settings or environment variables.
3. Use provider-qualified model names to avoid ambiguous routing.
4. Test one non-sensitive prompt before enabling the route broadly.

</Steps>

## Expected result

The command or interface reports completion in the selected scope. For automation, prefer its structured result instead of parsing decorated terminal output.

## Troubleshooting

Confirm the installed version and resolved configuration first. Retry with optional plugins, MCP servers, or extensions disabled when they could affect the path. Never print tokens while collecting diagnostics.

## Next steps

Continue to [Resolve secrets safely](../secrets/).
