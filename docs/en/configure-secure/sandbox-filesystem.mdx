---
title: "Sandbox and filesystem boundaries"
description: "Keep discovery and file tools inside the session workspace unless a path is explicitly granted."
sidebar:
  order: 7
  label: "Sandbox and filesystem boundaries"
---

import { Aside, Steps, TabItem, Tabs } from '@astrojs/starlight/components';

Keep discovery and file tools inside the session workspace unless a path is explicitly granted.

## Prerequisites

- A current xcsh release and a shell in the intended working directory.
- Authorization for any account, tenant, repository, or file the procedure touches.
- A backup or dry run before an operation that changes or deletes state.

## Procedure

<Steps>

1. Run xcsh from the narrowest useful working directory.
2. Verify the installed sandbox with `xcsh sandbox check`.
3. Grant an additional root with repeatable `--allow-path <path>` only when needed.
4. Use `--no-sandbox` only in an already isolated environment after reviewing the risk.

</Steps>

## Expected result

The command or interface reports completion in the selected scope. For automation, prefer its structured result instead of parsing decorated terminal output.

## Troubleshooting

Confirm the installed version and resolved configuration first. Retry with optional plugins, MCP servers, or extensions disabled when they could affect the path. Never print tokens while collecting diagnostics.

## Next steps

Continue to [Plan an enterprise deployment](../enterprise-deployment/).
