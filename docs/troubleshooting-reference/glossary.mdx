---
title: "Glossary"
description: "Define the product, model, session, context, manifest, skill, plugin, MCP, and sandbox terms used throughout the site."
sidebar:
  order: 10
  label: "Glossary"
---

import { Aside, Steps, TabItem, Tabs } from '@astrojs/starlight/components';

Define the product, model, session, context, manifest, skill, plugin, MCP, and sandbox terms used throughout the site.

- **Agent loop:** repeated model inference, tool execution, and result observation.
- **Context:** a named F5 Distributed Cloud endpoint, credential, and default namespace.
- **Manifest:** desired resource data with `kind`, `metadata`, and `spec`.
- **Session:** persisted conversation and tool-event history.
- **Skill:** discoverable instructions centered on a `SKILL.md` file.
- **Plugin:** installable bundle of xcsh capabilities and configuration.
- **MCP:** Model Context Protocol, used to connect out-of-process tools, resources, and prompts.
- **Sandbox:** filesystem discovery and access boundary enforced around a session.
- **Role:** a model-selection slot such as default, smol, slow, or plan.

## Prerequisites

- A current xcsh release and a shell in the intended working directory.
- Authorization for any account, tenant, repository, or file the procedure touches.
- A backup or dry run before an operation that changes or deletes state.

## Procedure

<Steps>

1. Use this page when a term is ambiguous.
2. Follow links from each definition to the operational guide.
3. Prefer terms emitted by the current CLI and source.
4. Report terminology drift with the exact page and installed version.

</Steps>

## Expected result

The command or interface reports completion in the selected scope. For automation, prefer its structured result instead of parsing decorated terminal output.

## Troubleshooting

Confirm the installed version and resolved configuration first. Retry with optional plugins, MCP servers, or extensions disabled when they could affect the path. Never print tokens while collecting diagnostics.

## Next steps

Return to the section overview and choose the next task that matches your workflow.
