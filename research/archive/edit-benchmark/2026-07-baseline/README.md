# TypeScript Edit Benchmark Baseline — July 2026

Status: historical result.

`results.json` is the sanitized aggregate that was previously stored beside the executable benchmark. It is retained for comparison only. Generated TypeScript fixtures are reproducible through `bun run bench:gen-fixtures` and are not archived.
