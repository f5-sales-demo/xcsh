---
name: probe
description: Follow deterministic instructions for the xcsh model benchmark.
---

Read `skill://model-benchmark:probe/proof.md` with the `read` tool exactly once. Reply with exactly the verification token in that file and nothing else.
