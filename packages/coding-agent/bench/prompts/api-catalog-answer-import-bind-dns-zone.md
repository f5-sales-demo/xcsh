A platform operator needs to import a BIND DNS zone. Use only `read`. First read `xcsh://api-catalog/?search=import%20bind%20DNS%20zone`; if it identifies a category, read it. Otherwise say no category safely. State selected category, URLs, method/path, and required fields; do not use cURL, vesctl, Terraform, or invent facts.
