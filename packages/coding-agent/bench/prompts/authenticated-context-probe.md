Using the `xcsh_api` tool exactly once, perform a read-only GET request to `/api/web/namespaces`. Do not call any other tools. Use the tool response's final `Item count` summary as the exact accessible namespace count; do not estimate from visible records. Report `Accessible namespace count: <integer>`, `Active tenant: <tenant>`, and `Active namespace: <namespace>`. Do not list namespace names or include credentials.
