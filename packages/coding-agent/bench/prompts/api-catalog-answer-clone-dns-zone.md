A platform operator needs to clone a DNS zone from a DNS domain. Use only the `read` tool and authoritative internal xcsh URLs. First read `xcsh://api-catalog/?search=clone%20a%20DNS%20zone`. If that result identifies a category, read the category it identifies before answering. If it identifies no category, say so safely rather than guessing.

Give a concise answer that names the selected category, cites every `xcsh://` URL read in order, states the HTTP method and path, and states the required fields. Do not use or recommend cURL, vesctl, or Terraform. Do not invent fields or paths.
