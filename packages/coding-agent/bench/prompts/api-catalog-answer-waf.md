A customer needs to create a Web Application Firewall policy. Use only the `read` tool and authoritative internal xcsh URLs. First read `xcsh://api-catalog/?search=web%20application%20firewall`; then read the selected catalog category; then read `xcsh://api-spec/virtual?resource=app_firewall`.

Give a concise evidence-grounded answer that names the selected resource, cites every `xcsh://` URL read in order, states the create HTTP method and path, and lists exactly the required fields. Do not use or recommend cURL, vesctl, or Terraform. Do not invent fields or paths.
