// Auto-generated by scripts/generate-extension-capabilities.ts - DO NOT EDIT

export const EXTENSION_TOOLS_REFERENCE = "# xcsh extension — tool signatures\n\n> Authoritative signatures generated from the extension's capability contract (contract 1.0.0). Use these exact params; pair with the guidance above for when/why to use each tool.\n\n## meta\n\n- `ping` _(read-only)_ — Liveness check; returns { ok, version }.\n- `capabilities` _(read-only)_ — Return this self-describing capability manifest (tools + features + versions).\n- `reload` _(mutates)_ — Reload the extension (re-reads dist/ from disk).\n- `debug_exec` _(read-only)_ — Diagnostic: probe in-page bridge availability.\n- `detach` _(mutates)_ — Detach the debugger from the target tab.\n\n## navigation\n\n- `navigate { url: string }` _(mutates)_ — Navigate the console tab to a scoped https URL.\n- `login { email: string, password: string, consoleUrl: string }` _(mutates)_ — Drive the F5 XC OIDC/Keycloak login end-to-end.\n- `scroll_to { ref: string }` _(mutates)_ — Scroll an AX-ref element into view.\n- `resize_window { width: number, height: number }` _(mutates)_ — Resize the browser window.\n- `tabs_list` — List scoped console tabs.\n- `tabs_create { url: string }` _(mutates)_ — Open a new tab at a scoped URL.\n- `tabs_close { tabId: number }` _(mutates)_ — Close a tab by id.\n\n## interaction\n\n- `click { ref: string }` _(mutates)_ — Deterministic click of an AX-ref element (layout-engine coords + hit-test).\n- `click_element { js: string, wait_ms?: number }` _(mutates)_ — Click the element returned by a JS expression (polls; occlusion-safe).\n- `click_xy { x: number, y: number }` _(mutates)_ — Trusted click at explicit viewport coordinates.\n- `type_text { text: string }` _(mutates)_ — Type text into the focused element (trusted input).\n- `form_input { ref: string, value: string }` _(mutates)_ — Set a form field value by AX ref.\n- `key_press { key: string }` _(mutates)_ — Dispatch a key press.\n- `select_option { ref: string, value: string }` _(mutates)_ — Select an option in a native <select> by AX ref.\n- `label_select { selector: string, value: string, label_value?: string, wait_ms?: number }` _(mutates)_ — Type into a CDK-portal typeahead and click the matching option.\n- `file_upload { ref: string, files: unknown[] }` _(mutates)_ — Upload files (base64 data URIs) to a file input by AX ref.\n\n## read\n\n- `read_ax` _(read-only)_ — Read the accessibility tree of the page.\n- `get_page_text` _(read-only)_ — Return the page text.\n- `find { selector: string }` _(read-only)_ — Find AX nodes matching a locator.\n- `wait_for { selector: string, context?: string, timeoutMs?: number }` _(read-only)_ — Wait for an AX node matching a locator to appear.\n- `assert_text { selector: string, expected: string, context?: string }` _(read-only)_ — Assert an element contains expected text.\n- `screenshot` _(read-only)_ — Capture a screenshot (base64 PNG).\n- `read_console { pattern?: string }` _(read-only)_ — Read buffered console messages, optionally filtered by pattern.\n- `read_network { pattern?: string }` _(read-only)_ — Read buffered network events, optionally filtered by pattern.\n- `wait_for_api_response { pattern?: string, timeout_ms?: number }` _(read-only)_ — Wait for a network response whose URL matches a pattern.\n\n## script\n\n- `javascript_tool { code: string }` _(mutates)_ — Evaluate arbitrary JS in the page (length-capped).\n- `browser_batch { actions: unknown[] }` _(mutates)_ — Run a batch of { tool, params } actions in sequence.\n\n## annotation\n\n- `set_explain_mode { enabled?: boolean }` — Enter/leave explain mode — the gate for all on-page annotation overlays.\n- `annotate { kind: string, ref?: string, x?: number, y?: number, w?: number, h?: number }` _(requires explain mode)_ — Draw an overlay annotation (fingerprint/highlight). No-op unless explain mode is on.\n";
