---
# Governed caller for the Claude PR reviewer. Synced by docs-control managed-files
# to opted-in (runner-equipped) repos only; other repos skip it via skip_files.
# Invokes the shared reusable reviewer in docs-control at @main (fleet convention).
name: Code Review
on:
  pull_request:
    types: [opened, synchronize, reopened]

# Per-PR dedup: a new push cancels the superseded in-flight review for the SAME
# PR, so two reviews of one PR never overlap. Concurrency groups are scoped per
# repository, so this key needs no repo component (PR #5 in two repos never collide).
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number }}
  cancel-in-progress: true

permissions: {}

jobs:
  review:
    # Real agentic review for HUMAN, same-repo PRs. Skipped for genuine automated
    # branches (handled by review-status-automated below) so fleet automation is
    # not gated on model inference. Skipping a reusable-workflow caller does NOT
    # satisfy a required `review / claude-review` context (the nested context is
    # never produced), so the skip set here MUST exactly mirror the automated set
    # below — anything skipped here must get its status posted there, or the PR
    # deadlocks.
    #
    # The automated set mirrors require-linked-issue.yml's EXCLUDE_BRANCHES
    # (dependabot, release, openapi-sync, plugin-sync, deps, sync, docs/update-*)
    # plus governance/, auto-regenerate/, auto-generate/, autoresearch/ — so a
    # machine-generated PR is bypassed by BOTH gates consistently instead of
    # burning a real Opus review.
    #
    # dependabot/ requires Dependabot's immutable actor ID: a human naming a branch
    # `dependabot/x` therefore gets the REAL review, not a free pass. The other
    # prefixes are created by sync/automation via a human-owned PAT
    # (REPO_SYNC_TOKEN), so github.actor is a human account and cannot distinguish
    # a genuine automated PR from a person naming the branch — a prefix check is
    # the best signal available here. RESIDUAL RISK: a write-access contributor
    # could bypass the required review by using one of those prefixes. Mitigated by
    # trusted-write-access + the reusable's fork guard; full closure requires a
    # dedicated bot/App identity for the automation or a branch-creation ruleset
    # restricting who may push those prefixes.
    #
    if: >-
      github.event.pull_request.head.repo.full_name == github.repository &&
      !(startsWith(github.head_ref, 'governance/') ||
        startsWith(github.head_ref, 'sync/') ||
        startsWith(github.head_ref, 'release/') ||
        startsWith(github.head_ref, 'openapi-sync/') ||
        startsWith(github.head_ref, 'plugin-sync/') ||
        startsWith(github.head_ref, 'deps/') ||
        startsWith(github.head_ref, 'docs/update-') ||
        startsWith(github.head_ref, 'auto-regenerate/') ||
        startsWith(github.head_ref, 'auto-generate/') ||
        startsWith(github.head_ref, 'autoresearch/') ||
        (startsWith(github.head_ref, 'dependabot/') && github.event.pull_request.user.id == 49699333))
    uses: f5-sales-demo/docs-control/.github/workflows/claude-review.yml@94a0cb518b88f717e7b452a714fe7f97a00d0bbb
    permissions:
      contents: read # inspect the same-repository pull request
      pull-requests: write # publish the reusable review result
    secrets:
      F5_GATEWAY_TOKEN: ${{ secrets.F5_GATEWAY_TOKEN }}
      REPO_SETTINGS_TOKEN: ${{ secrets.REPO_SETTINGS_TOKEN }}

  # Genuine automated branches skip the reviewer above (never touching the
  # model reviewer), so the required `review / claude-review` context would
  # never post and the PR would deadlock. Post it as a passing commit status on
  # exactly those branches (same-repo only, so GITHUB_TOKEN can write and forks
  # are excluded). Condition MUST mirror the negated `review` condition above.
  # The status description explicitly records the exemption; it does not imply
  # that a model review ran.
  review-status-automated:
    name: Mark Automated Review Exemption
    if: >-
      github.event.pull_request.head.repo.full_name == github.repository &&
      (startsWith(github.head_ref, 'governance/') ||
       startsWith(github.head_ref, 'sync/') ||
       startsWith(github.head_ref, 'release/') ||
       startsWith(github.head_ref, 'openapi-sync/') ||
       startsWith(github.head_ref, 'plugin-sync/') ||
       startsWith(github.head_ref, 'deps/') ||
       startsWith(github.head_ref, 'docs/update-') ||
       startsWith(github.head_ref, 'auto-regenerate/') ||
       startsWith(github.head_ref, 'auto-generate/') ||
       startsWith(github.head_ref, 'autoresearch/') ||
       (startsWith(github.head_ref, 'dependabot/') && github.event.pull_request.user.id == 49699333))
    runs-on: ubuntu-latest
    permissions:
      statuses: write # post the required review context for trusted automation
    steps:
      - name: Post passing review/claude-review status for automated PR
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
          SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          gh api -X POST "repos/${REPO}/statuses/${SHA}" \
            -f state=success \
            -f context='review / claude-review' \
            -f description='Automated PR: AI review not required'
